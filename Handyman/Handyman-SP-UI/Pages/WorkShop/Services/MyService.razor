@page "/my-service"
@page "/servicist"

@using Microsoft.AspNetCore.Components.Web
@inject NavigationManager NavManager
@inject IProviderHelper _providerHelper
@inject IJSRuntime JSRuntime



@code{

    // [Parameter]
    // public ServiceModel? serviceModel { get { return svc; } set { svc = value; } }



    // bool isAdded; 
    // ServiceModel? svc;

    // protected override async Task OnInitializedAsync()
    // {
    //     try
    //     {

    //         if(svc!=null)
    //         {
    //             if(await _providerHelper.IsServiceProvided(svc))
    //             {
    //                 isAdded = true;
    //                 StateHasChanged();

    //             }
    //         }

    //     }catch(Exception ex)
    //     {
    //         throw new Exception(ex.Message, ex.InnerException);
    //     }


    // }

    // /// <summary>
    // /// Add this service to the provider's services
    // /// </summary>
    // /// <returns></returns>
    // /// <exception cref="Exception"></exception>
    // async Task AddService(ServiceModel serviceModel)
    // {

    //     try
    //     {
    //         ServiceProviderModel providerModel = new ServiceProviderModel();
    //         providerModel.Services.Add(serviceModel);
    //         await _providerHelper.AddService(serviceModel);
          
    //        await InvokeAsync(StateHasChanged);

    //     }
    //     catch (Exception ex)
    //     {
    //         throw new Exception(ex.Message, ex.InnerException);
    //     }
    // }


}
