@inject NavigationManager NavigationManager

@code {
    [Parameter]
    public string? ReturnUrl { get; set; }
    protected override void OnInitialized()
    {
        ReturnUrl = "~/" + ReturnUrl;
        NavigationManager.NavigateTo($"/sign-in?returnUrl={ReturnUrl}",
           forceLoad: true);
    }
}